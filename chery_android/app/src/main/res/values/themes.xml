<!--
    themes.xml文件是Android应用中用于定义和管理应用主题样式的配置文件
    主题控制着应用的整体外观，包括颜色、字体、控件样式等

    这个文件位于res/values/目录下，定义了应用在默认（浅色）模式下的主题样式
    与之对应的还有res/values-night/目录下的themes.xml，用于深色模式
-->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 
        原始应用基础主题（已不再使用）
        parent属性指定了这个主题继承自Material Components库的DayNight.DarkActionBar主题
        DayNight表示支持日间/夜间模式切换，DarkActionBar表示带有深色的ActionBar
    -->
    <style name="Theme.Chery" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- 品牌主色调，通常用于ActionBar、按钮等关键UI元素 -->
        <item name="colorPrimary">@color/purple_500</item>
        
        <!-- 主色调的变体，通常用于强调或阴影效果 -->
        <item name="colorPrimaryVariant">@color/purple_700</item>
        
        <!-- 用于主色调背景上的文本颜色，确保可读性 -->
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- 品牌次要颜色，用于次要UI元素 -->
        <item name="colorSecondary">@color/teal_200</item>
        
        <!-- 次要颜色的变体 -->
        <item name="colorSecondaryVariant">@color/teal_700</item>
        
        <!-- 用于次要颜色背景上的文本颜色 -->
        <item name="colorOnSecondary">@color/black</item>
        
        <!-- 状态栏颜色，这里引用了colorPrimaryVariant的值 -->
        <!-- ?attr/ 语法表示引用当前主题中的属性值 -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        
        <!-- 可以在这里添加更多自定义主题属性 -->
    </style>

    <!-- 
        全屏主题定义
        继承自AppCompat库的Light.NoActionBar主题（浅色主题，无ActionBar）
        这个主题可能用于特定的全屏界面
    -->
    <style name="Theme.App.Fullscreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 设置ActionBar是否覆盖在内容之上 -->
        <item name="android:windowActionBarOverlay">true</item>
        
        <!-- 设置窗口背景为null -->
        <item name="android:windowBackground">@null</item>
        
        <!-- 设置全局背景色为半透明黑色(#66000000) -->
        <!-- 十六进制颜色格式：#AARRGGBB，AA表示透明度(00完全透明，FF完全不透明) -->
        <item name="android:background">#66000000</item>
    </style>

    <!-- 
        应用的主要主题 - 无ActionBar版本
        这是当前应用正在使用的主题（在AndroidManifest.xml中配置）
        继承自AppCompat.Light.NoActionBar，确保在不同Android版本上都有一致的外观
    -->
    <style name="Theme.App" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- AppCompat库中的主色调属性 - 改为白色以避免顶部显示紫色条 -->
        <item name="colorPrimary">@color/white</item>
        
        <!-- AppCompat库中的主色调深色变体 - 也改为白色 -->
        <item name="colorPrimaryDark">@color/white</item>
        
        <!-- AppCompat库中的强调色，保持不变 -->
        <item name="colorAccent">@color/teal_200</item>
        
        <!-- 明确设置状态栏颜色为透明，确保不显示颜色条 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>